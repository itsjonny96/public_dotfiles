alias v='nvim'
alias l='colorls -lah'
alias ls='colorls'
alias inv='nvim $(fzf --preview="bat --color=always {}")'

# Machine-specific aliases (QMK keyboard paths, project shortcuts, etc.)
# go in ~/dotfiles/zsh/local.zsh — that file is gitignored and never tracked.
